<odoo>
    <template id="timesheet_table" inherit_id="hr_timesheet.timesheet_table">
        <xpath expr="//thead/tr/th[@t-if='show_project']" position="before">
            <th class="text-start align-middle" t-if="show_claim"><span
        >Claim</span></th>
        </xpath>
        <xpath expr="//tr[@t-as='line']//td[@t-if='show_project']" position="before">
            <td t-if="show_claim" class="align-middle">
                <span t-esc="line.claim_id.sudo().display_name" />
            </td>
        </xpath>
        <t t-set="nbCols" position="after">
            <t t-if="show_claim" t-set="nbCols" t-value="nbCols + 1" />
        </t>
    </template>

    <template id="report_timesheet" inherit_id="hr_timesheet.report_timesheet">
        <t t-set="show_task" position="after">
            <t t-set="show_claim" t-value="bool(docs.mapped('claim_id'))" />
        </t>
    </template>

</odoo>
